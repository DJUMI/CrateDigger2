general
    cache assets
    add custom fonts?
    create loading animation
navigation
    make createNavigationOptions function to clean up main tab navigator
home
    handle case for empty home list
search
    figure out why slider thumb is cut off
dig
    restart swiper when last card reached
    maybe lower height on card image? need to test with actually images
    filter button should be rendered before swiper
cart
    update swipeable to use effects
    add cart to async storage
    fix buttons
scaling
    main tab bar icons are cutoff
    navigation header back button
    dig card is a little wonky on ipad